import { ReactiveFormsModule } from '@angular/forms';
import { AutoFormErrorDirective } from './directives/auto-form-error.directive';

export const CoreFormsImports = [ReactiveFormsModule, AutoFormErrorDirective];
